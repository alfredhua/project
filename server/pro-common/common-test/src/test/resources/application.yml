spring:
  application:
    name: common-test
  config:
    import:
#      - classpath:/config/redis.properties
#      - classpath:/config/rabbitmq.properties
      - classpath:/config/zk.properties

server:
  port: 4001
