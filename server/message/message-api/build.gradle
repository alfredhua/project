dependencies {
    compile project(':common:common-spring-boot-starter')
}


group(project.name)
version("$projectVersion")

publishing {
    publications {
        maven(MavenPublication) {
            groupId = 'com.message.wechat'
            artifactId = project.name
            version = "$projectVersion"
            from components.java
        }
    }
}